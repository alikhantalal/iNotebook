import React from "react";
import Notes from './Notes'; // Corrected the path to import Notes component

const Home = () => {
    return (
       <div>

        <Notes/>
            </div>
        
    );
};

export default Home;
